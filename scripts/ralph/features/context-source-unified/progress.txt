iter 2026-05-29T00:00:00Z: PASS CTXSRC-001
  files: supabase/migrations/20260530a_create_context_source.sql, src/lib/supabase/database.types.ts
  learnings: Migration executada via psql DIRECT_URL. Enum context_source_kind com 7 valores. 4 RLS policies (select/insert/update/delete). Types regenerados via npm run db:types (necessita project-id, não db-url).
Perfect! Commit successful: `fc2f0c3`

---

## Ralph Iteration 1 — Summary

**Story:** CTXSRC-001 — Migration A: criar tabela ContextSource + enum + RLS

**Verificações executadas:**
- ✅ Migration file exists
- ✅ Table `ContextSource` created in DB
- ✅ Enum `context_source_kind` with 7 values
- ✅ 4 RLS policies (select, insert, update, delete)
- ✅ `database.types.ts` updated with ContextSource type
- ✅ TypeScript compilation passes

**Commit:** `fc2f0c3` — pushed to both origin and staging remotes

**Próximo passo sugerido:** CTXSRC-002 (Migration B — DesignSessionContextLink + RLS) is now eligible, as its only dependency (CTXSRC-001) has `passes: true`.

iter $(date -u +%Y-%m-%dT%H:%M:%SZ): PASS CTXSRC-002
  files: supabase/migrations/20260530b_create_design_session_context_link.sql, src/lib/supabase/database.types.ts
  learnings: RLS usa can_view_design_session (não can_view_session). Ribbon refactor pré-existente não bloqueou migration. db:types via project-id funciona mesmo com psql tendo falha de socket local.
✅ Commit `fb4922e` criado e pushado para `origin` e `staging`.

---

## Ralph Iteration 2 — Summary

**Story:** CTXSRC-002 — Migration B: DesignSessionContextLink + RLS

**Verificações executadas:**
- ✅ Migration file `20260530b_create_design_session_context_link.sql` criado
- ✅ Tabela `DesignSessionContextLink` criada no DB com FK, unique constraint, 4 RLS policies
- ✅ `database.types.ts` atualizado (8 ocorrências de DesignSessionContextLink)
- ✅ TypeScript compilation passes

**Ajustes realizados:**
- Corrigido RLS policy: `can_view_session` → `can_view_design_session` (descoberto via query de funções existentes)
- Drop e recriação da tabela após erro parcial na primeira tentativa
- Tipos regenerados via `npm run db:types` (project-id method)

**Commit:** `fb4922e` — pushed to both origin and staging remotes

**Próximo passo sugerido:** CTXSRC-003 (Migration C — PMReviewContextLink) e CTXSRC-004 (backfill TranscriptRef) são agora elegíveis, ambas dependem apenas de CTXSRC-001 que já tem `passes: true`. CTXSRC-003 tem menor id lexicográfico.
iter 2026-05-30T02:26:20Z: PASS CTXSRC-003
  files: supabase/migrations/20260530c_create_pm_review_context_link.sql, src/lib/supabase/database.types.ts
  learnings: RLS usa can_create_pm_review (helper existente) + can_view_project. Padrão espelhado de DesignSessionContextLink. source <(...) falhou com psql, necessário DIRECT_URL hardcoded.
---

## Ralph Iteration 3 — Summary

**Story escolhida:** CTXSRC-003 — Migration C: PMReviewContextLink + RLS + helpers can_view/edit_pm_review

**Verificações executadas:**
- ✅ Migration file `20260530c_create_pm_review_context_link.sql` criado
- ✅ Tabela `PMReviewContextLink` criada no DB com FK, unique constraint
- ✅ RLS habilitada com 4 policies (select, insert, update, delete)
- ✅ Helpers `can_create_pm_review` e `can_view_project` já existiam (reutilizados)
- ✅ `database.types.ts` atualizado (7 ocorrências de PMReviewContextLink)
- ✅ TypeScript compilation passes

**Ajustes técnicos:**
- Helper `can_create_pm_review` já existia desde migration 20260529d_pm_review.sql (não foi necessário criar)
- Padrão RLS espelhado de `DesignSessionContextLink` (4 policies com lógica similar)
- Workaround: `source <(grep...)` falhou com psql; necessário usar `DIRECT_URL` hardcoded

**Commit:** `33495ca` — pushed to origin and staging remotes

**Próximo passo sugerido:** CTXSRC-004 (Migration D — backfill TranscriptRef → ContextSource) e CTXSRC-005 (Migration E — backfill Meetings) são agora elegíveis, ambas dependem apenas de CTXSRC-001 (passes: true). CTXSRC-004 tem menor id lexicográfico.
iter 2026-05-30T02:29:48Z: PASS CTXSRC-004
  files: supabase/migrations/20260530d_backfill_transcripts_into_context_source.sql, scripts/ralph/features/context-source-unified/prd.json
  learnings: Backfill idempotente preserva id via ON CONFLICT (id) DO NOTHING. 18 TranscriptRef migrados. payload jsonb carrega source/sourceId/byline/meetingId. fullText preservado, summary=NULL (não existe em TranscriptRef). Workaround psql: DIRECT_URL hardcoded funciona, source <(...) falha com socket local.
---

## Ralph Iteration 4 — Summary

**Story escolhida:** CTXSRC-004 — Migration D: backfill TranscriptRef → ContextSource

**Verificações executadas:**
- ✅ Migration file `20260530d_backfill_transcripts_into_context_source.sql` criado
- ✅ Migration executada via psql (18 TranscriptRef rows backfilled)
- ✅ Count verificado: TranscriptRef == ContextSource WHERE kind='transcript' (ambos retornam 18)
- ✅ ID preservation verificado: todos os ids de TranscriptRef existem em ContextSource com mesmo id e kind='transcript'
- ✅ Idempotência garantida via `ON CONFLICT (id) DO NOTHING`

**Implementação:**
- Migration preserva id via INSERT ... ON CONFLICT (id) DO NOTHING para manter FKs externas válidas
- Mapeia TranscriptRef → ContextSource:
  - kind = 'transcript'
  - title = COALESCE(title, byline, 'Transcript sem título')
  - externalId = sourceId (roamId/granolaId)
  - payload jsonb = {source, sourceId, byline, meetingId}
  - fullText preservado
  - summary = NULL (não existe em TranscriptRef)
  - createdBy/createdAt mapeados de importedById/importedAt

**Ajustes técnicos:**
- Workaround psql: `source <(grep...)` falha com socket local; necessário usar DIRECT_URL hardcoded
- 18 TranscriptRef rows migrados com sucesso

**Commit:** `c11c33e` — pushed to origin and staging remotes

**Próximo passo sugerido:** CTXSRC-005 (Migration E — backfill Meetings) e CTXSRC-008 (Adapters base) são agora elegíveis, ambas dependem apenas de CTXSRC-001 (passes: true). CTXSRC-005 tem menor id lexicográfico.
iter 2026-05-30T02:32:50Z: PASS CTXSRC-005
  files: supabase/migrations/20260530e_backfill_meetings_into_context_source.sql
  learnings: Meeting schema difere do PRD (date não startsAt, notes não summary). JOIN via PMReviewMeetingLink→PMReview pra obter projectId. 1 meeting backfilled. Idempotência via NOT EXISTS (não há unique constraint kind+externalId).
