iter 2026-05-31T$(date -u +%H:%M:%S)Z: PASS FDS-001
  files: scripts/forge/daemon-ctl.sh
  learnings: daemon-ctl.sh usa nohup+disown pra detach do shell; shellcheck instalado via brew; PID file em ~/.forge/daemon.pid permite status check idempotente
---

## Ralph Iteration Summary

**Story chosen:** FDS-001 — Implementar daemon-ctl.sh com start/stop/status

**Implementation:**
- Created `scripts/forge/daemon-ctl.sh` with bash script implementing three core commands:
  - `start`: Uses `nohup` + `disown` to detach daemon process, redirects output to `~/.forge/daemon.log`, writes PID to `~/.forge/daemon.pid`, handles stale PID files, prevents duplicate starts
  - `stop`: Sends SIGTERM, waits up to 10s for graceful shutdown, sends SIGKILL if needed, cleans PID file
  - `status`: Shows running state, PID, uptime (via `ps -o etime`), and last heartbeat from DB (if available)
- Made script executable (`chmod +x`)
- Installed shellcheck via Homebrew to satisfy verification requirements

**Verifications run:**
1. ✓ `test -x scripts/forge/daemon-ctl.sh && echo OK` → "OK"
2. ✓ `shellcheck scripts/forge/daemon-ctl.sh 2>&1 | wc -l` → "0"

Both checks passed.

**Commit hash:** 027de35

**Next step:** Iteration complete. Next eligible stories are FDS-002 (depends on FDS-001, now unblocked) and FDS-003 (no dependencies). Ralph will pick FDS-002 next (lexicographically first among eligible).
iter 2026-05-31T21:48:48Z: PASS FDS-002
  files: scripts/forge/daemon-ctl.sh
  learnings: logs usa tail -F com -n <lines> + -f/--follow; restart chama stop + start sequencialmente; case statement passa args com shift + "$@"
---

## Iteração Ralph 2 - Relatório

**Story escolhida:** FDS-002 — Adicionar comandos logs e restart

**Implementação:**
- Adicionei função `show_logs()` ao `scripts/forge/daemon-ctl.sh`:
  - Suporta `-n <lines>` para especificar número de linhas (default: 50)
  - Suporta `-f/--follow` para seguir logs em tempo real (`tail -F`)
  - Valida existência do arquivo de log
- Adicionei função `restart_daemon()`:
  - Chama `stop_daemon` (bloqueia até morte do processo)
  - Sleep 1s para garantir limpeza
  - Chama `start_daemon` (bloqueia até novo PID)
- Atualizei o case statement principal para incluir `logs)` e `restart)` com passagem de argumentos via `shift + "$@"`

**Verificações executadas:**
1. ✓ `grep -cE 'logs\)|restart\)' scripts/forge/daemon-ctl.sh` → "2" (esperado: 2)
2. ✓ `shellcheck scripts/forge/daemon-ctl.sh 2>&1 | wc -l` → "0" (esperado: 0)

Ambos os checks passaram.

**Commit hash:** 6170a5b

**Próximo passo:** Próximas stories elegíveis são FDS-003 (sem dependências) e FDS-004 (agora desbloqueada, depende de FDS-002). Ralph escolherá FDS-003 (menor lexicograficamente).
iter 2026-05-31T21:50:54Z: PASS FDS-003
  files: scripts/forge/daemon.ts
  learnings: handleShutdown() agora loga timestamp ISO com 'shutdown signal received' pra debug; tsc path aliases não resolvem sem tsconfig.json mas --project tsconfig.json resolve
---

## Ralph Iteração 3 - Relatório

**Story escolhida:** FDS-003 — daemon.ts loga shutdown signal antes do exit

**Implementação:**
- Adicionei log de timestamp ISO com mensagem "shutdown signal received" na função `handleShutdown()` em `scripts/forge/daemon.ts` (linha 277-279)
- A mensagem aparecerá em `~/.forge/daemon.log` quando o daemon receber SIGTERM/SIGINT via `daemon-ctl.sh stop`
- Mudança cirúrgica: apenas 2 linhas adicionadas

**Verificações executadas:**
1. ✓ `npx tsc --noEmit --project tsconfig.json` → sem erros em daemon.ts (0 erros reportados)
2. ✓ `grep -c 'shutdown signal received' scripts/forge/daemon.ts` → "1" (esperado: 1)

Ambos os checks passaram.

**Commit hash:** b50e9f0

**Próximo passo:** Próximas stories elegíveis são FDS-004 (depende de FDS-002, já passou) e FDS-005 (depende de FDS-001, já passou). Ralph escolherá FDS-004 (menor lexicograficamente).
iter $(date -u +%Y-%m-%dT%H:%M:%S)Z: PASS FDS-004
  files: docs/runbooks/forge-daemon-runbook.md
  learnings: Runbook específico criado (não bloco em AGENTS.md) — padrão: doc nova vai em subpasta certa de docs/runbooks/, nunca solto na raiz
---

## Ralph Iteration 4 — Report

**Story escolhida:** FDS-004 — Documentação do daemon-ctl

**Implementação:**
- Criado `docs/runbooks/forge-daemon-runbook.md` com documentação completa do daemon:
  - Tabela de localização (CLI, source, PID file, logs, DB heartbeat)
  - Exemplos de todos os comandos (start/stop/status/logs/restart)
  - Descrição detalhada do comportamento de cada comando (exit codes, timeout, detach, etc.)
  - Troubleshooting table (PID stale, crashes, DB unavailable, permissões, SIGKILL)
  - Smoke test example
  - Convenções de desenvolvimento (migrations, linting, typecheck, commit)
  - Referências pra outros runbooks

**Verificações executadas:**
1. ✓ `grep -l 'daemon-ctl.sh' docs/runbooks/forge-daemon-runbook.md AGENTS.md 2>/dev/null | wc -l` → "1" (esperado: 1)

Check passou.

**Commit hash:** 5710162

**Próximo passo:** FDS-005 agora é a única story com `passes=false`. Depende de FDS-001 (já passou). Next iter executará FDS-005 (smoke test de detach + sobrevivência).
iter 2026-05-31T21:55:10Z: PASS FDS-005
  files: scripts/forge/test-smoke-daemon-detach.sh
  learnings: smoke test subshell detach usa wait + trap cleanup; heartbeat DB check com retry loop 10s; daemon sobrevive exit do parent shell via nohup+disown
---

## Ralph Iteração 5 — Relatório Final

**Story escolhida:** FDS-005 — Smoke test detach + sobrevivência

**Implementação:**
- Criado `scripts/forge/test-smoke-daemon-detach.sh` com teste automatizado end-to-end:
  - Inicia daemon em um subshell isolado
  - Aguarda finalização do subshell (via `wait`)
  - Verifica que daemon continua rodando após morte do subshell (validação da técnica `nohup + disown`)
  - Tenta verificar heartbeat no DB (com retry loop de 10s, opcional se `DIRECT_URL` disponível)
  - Trap `cleanup()` garante que daemon é sempre parado ao final do teste
  - Script executável (`chmod +x`)
  - Output colorizado com status PASS/FAIL explícito

**Verificações executadas:**
1. ✓ `bash scripts/forge/test-smoke-daemon-detach.sh` → "✓ PASS: Daemon survived subshell exit"

Check passou. O daemon sobreviveu corretamente ao exit do shell que o lançou.

**Commit hash:** a3fbd68

**Próximo passo:** **Feature COMPLETA** — todas as 5 stories (FDS-001 a FDS-005) agora têm `passes: true`. O PRD `forge-daemon-service` está pronto para Checkpoint Humano ou Closeout. Ralph pode mover o PRD de `in-progress/` para `blocked/` (checkpoint) ou `done/` (closeout).
